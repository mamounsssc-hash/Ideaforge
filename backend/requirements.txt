# Install with:  pip install -r requirements.txt
# Versions are minimums (>=) so pip can pick wheels that match your Python.

# ---- Core web (required) ----
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
websockets>=12
pydantic>=2.6
pydantic-settings>=2.2

# ---- Media / ML (required for clipping) ----
faster-whisper>=1.0        # local, free speech-to-text with word timestamps
yt-dlp>=2024.8.6           # download from YouTube/TikTok/etc.
numpy>=1.26
opencv-python-headless>=4.9   # face detection / reframe (works without mediapipe)

# ---- LLM (optional, all local by default) ----
httpx>=0.27                # talks to Ollama / LM Studio / Qwen3-VL (OpenAI-compatible)

# ---- Text-to-speech for the faceless "Create" mode (free) ----
edge-tts>=6.1              # free neural voices (online). Offline: use piper.

# ---- Optional: better face tracking. Safe to skip — OpenCV is the fallback. ----
# If this line fails to install (common on the newest Python), just remove it.
mediapipe>=0.10 ; python_version < "3.13"

# ---- Optional: real speaker diarization (who spoke when). ~1-2 GB of models. ----
# Needs a free HuggingFace token (IDEAFORGE_HF_TOKEN) + accepting the model terms.
# Uncomment to enable; otherwise the app uses the fast pause heuristic.
# pyannote.audio>=3.1

# Notes:
# - ffmpeg + ffprobe must be installed on the system (see START.bat / README).
# - Everything above is free and runs locally. No paid API keys required.
